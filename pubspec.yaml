name: labilletera
description: MVP Flutter application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.0
  go_router: ^12.0.0
  dio: ^5.3.0
  shared_preferences: ^2.2.0
  json_annotation: ^4.8.0
  equatable: ^2.0.5
  dartz: ^0.10.1
  flutter_secure_storage: ^9.0.0
  connectivity_plus: ^5.0.0
  logger: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  json_serializable: ^6.7.0
  mockito: ^5.4.0
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
